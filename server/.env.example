# Merge: 1 = comprimir (menor arquivo, upload mais rápido); 0 = copy (sem recompressão)
COMPRESS_VIDEO=1
# Preset: veryfast (mais rápido), fast (balanceado), medium, slow (melhor qualidade)
COMPRESS_PRESET=fast
# CRF: 18-23 (menor = melhor qualidade). 20 = bom para aulas, arquivos menores
COMPRESS_CRF=20

# Cloudflare R2 - necessário para a API de gravações
R2_ACCOUNT_ID=seu_account_id
R2_ACCESS_KEY=sua_access_key
R2_SECRET_KEY=sua_secret_key
R2_BUCKET=livebridge
