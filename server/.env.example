# Merge: 1 = H.264 CRF 18 + AAC 96k; 0 = copy (mais rápido, sem recompressão)
COMPRESS_VIDEO=1
# Preset: fast (equilíbrio qualidade/velocidade), faster, veryfast (mais rápido), medium, slow (melhor compressão)
COMPRESS_PRESET=fast

# Cloudflare R2 - necessário para a API de gravações
R2_ACCOUNT_ID=seu_account_id
R2_ACCESS_KEY=sua_access_key
R2_SECRET_KEY=sua_secret_key
R2_BUCKET=livebridge
