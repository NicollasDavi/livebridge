server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    pipeline_stages:
      - docker: {}
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - action: keep
        source_labels: [__meta_docker_container_name]
        # Docker devolve o nome com barra; alguns runtimes têm-na opcional
        regex: "^(/)?(livebridge-.*)$"
      - source_labels: [__meta_docker_container_id]
        action: replace
        regex: (.+)
        target_label: __path__
        replacement: /var/lib/docker/containers/${1}/${1}-json.log
      - source_labels: [__meta_docker_container_name]
        action: replace
        regex: "/?(.*)"
        target_label: container
      - target_label: job
        replacement: livebridge-logs
