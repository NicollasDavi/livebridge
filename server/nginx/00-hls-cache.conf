# Contexto http (conf.d). Segmentos .ts são idênticos para todos os espectadores do mesmo stream.
# keys_zone ~1M entradas; ajustar max_size ao disco disponível no host.

proxy_cache_path /var/cache/nginx/hls levels=1:2 keys_zone=hls_segment_cache:256m max_size=24g inactive=70m use_temp_path=off;
