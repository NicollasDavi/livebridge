# Stream output (segmentos HLS)
server/stream/*
!server/stream/.gitkeep

# Gravações (upload para R2)
server/recordings/*
!server/recordings/.gitkeep

# Certificados SSL autoassinados (gerar com scripts/generate-self-signed-cert.sh)
server/certs/

# Node
node_modules/

# Docker
.env.local

# Variáveis locais e chaves (OpenAI, R2, etc.): nunca comitar. Manter os `.env.example` no repo.
**/.env
!**/.env.example

# OS
.DS_Store
Thumbs.db
